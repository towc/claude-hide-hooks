---
description: Revert the hide-hooks patch and restore original Claude behavior
---

Reverting the hide-hooks patch...

This will restore the Claude binary from backup and make hook messages visible again.

```bash
${CLAUDE_PLUGIN_ROOT}/revert-patch.sh
```
